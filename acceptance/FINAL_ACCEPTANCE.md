# Novel Agent OS 最终验收

验收日期：2026-07-12  
真实项目：`人间种 · 24H production acceptance`  
项目 ID：`71d506bd-11fe-4489-85a9-52228701b42b`  
连续生产 Run ID：`a6b45385-b8a1-4c7e-977a-dc3d2beac475`

## 真实生产结果

本次验收使用真实兼容 API 与真实项目大纲完成，不是 Mock 或烟雾测试。

| 章节 | 最终字数 | 最终主编门禁 | 状态 |
|---|---:|---:|---|
| 第 1 章 | 3,541 | 88 | 已验收 |
| 第 2 章 | 4,765 | 88 | 已验收 |
| 第 3 章 | 2,742 | 88 | 已验收 |

连续生产终态为 `completed / stopped`，目标 3 章、完成 3 章、剩余 0 章，
`last_error = null`。第三章最终版本通过连续性检查，最终主编确定性门禁已持久化。

## 24H 与恢复验收

- 连续生产意图、进度、退避、熔断、半开、租约、心跳、代次与 fencing token 全部持久化。
- 写作中强杀后端后，watchdog 自动接管：Run ID 与 generation 不变，fencing token 正常递增，章节进度不回退。
- 原生 Windows 监督层强杀前端与后端后均自动生成新进程并恢复健康。
- 强杀监督进程后，计划任务巡检自动拉起新监督进程，并接管仍然健康的原前后端进程，没有重复实例。
- 当前机器已注册并启用 `NovelAgentOS-24H` 计划任务；登录触发与每分钟幂等巡检共同保证常驻。
- SQLite 在常驻切换前已保存一致性快照；Provider 密钥不进入任务参数和进程命令行。

## 自动质量、改错与学习

- 每章执行规划、起草、Critic、连续性守卫、重写、主编门禁和记忆更新。
- 低于阈值或连续性失败时只重试同一章，不会带病推进下一章。
- 不可变候选版本、历史最佳版本选择、回滚、问题生命周期和提示词来源均可审计。
- 第三章验证了跨重试历史最佳保护、指定历史版本修订、精确改错、重新质检和最终门禁闭环。
- 已完成 3 个学习周期，人工修订反馈均已处理，并沉淀 9 条 lesson 与 3 条 preference 长期记忆。
- 质量问题中没有 `open` 阻断项；历史问题已 resolved、superseded 或降为 advisory。

## 大纲、提示词与 UI

- 上传/更换大纲支持 `.docx`、`.txt`、`.md`，当前真实大纲保留 42,345 字并解析为 1 卷、10 个节拍、10 章。
- 新建项目同时提供“从零开始”和“上传已有大纲”，项目提示词在创建页和创作舱均有显式入口。
- 当前项目提示词已保存 559 字，并参与各 Agent 的生产提示词与来源记录。
- 创作舱提供 24H 总控、团队状态、章节导航、人工接管、恢复、质检中心与运行时间线。
- 桌面端与 390×844 移动端已实拍验收；核心入口在移动端保持可见且无异常溢出。

## 工程验证

- 后端：`104 passed`，Ruff 全部通过。唯一 warning 来自第三方 Starlette multipart 兼容导入。
- 前端：TypeScript 检查通过，Vite 正式构建通过（1,707 modules）。
- 视觉：创作舱、故事线、治理、进化、新建项目与移动端全部完成自动截图，`errors = []`。
- 数据：重复章节 0、重复版本 0、待处理审阅 0、运行中的残留连续会话 0。
- 常驻：后端健康检查与正式前端服务均为 HTTP 200。

## 运维入口

```powershell
Set-Location 'F:\kelaode\Data\Agents\zhongji8633\wudi8633\nos_staging'

# 查看当前原生 24H 状态
.\deploy\status-native-24h.ps1

# 停止并禁用登录自启（不删除任何数据）
.\deploy\stop-native-24h.ps1

# 更新代码后重新构建、备份并常驻启动
.\deploy\start-native-24h.ps1
```

Docker 可用的环境仍可使用 `deploy/start-24h.ps1`，详见根目录 `DEPLOYMENT.md`。
